Main file with form - index.php

For compilation of styles and scripts:

1) go in folder "assets".
2) Run command "npm install" in terminal for install packages
3) Run command "gulp" for compile styles and scripts and copy images or run command  "gulp watch" for watching styles, images and scripts.

After compiled all files will be created in folder "dist".
